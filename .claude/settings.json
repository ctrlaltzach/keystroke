{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash(git push *)",
        "hooks": [
          {
            "type": "command",
            "command": "branch=$(git rev-parse --abbrev-ref HEAD) && if [ \"$branch\" != \"main\" ]; then gh pr create --base main --head \"$branch\" --fill 2>/dev/null || true; fi",
            "statusMessage": "Creating PR..."
          }
        ]
      }
    ]
  }
}
